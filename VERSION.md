# 版本历史 (Version History)

本文档介绍每个版本实现的功能。

---

## v1

**提交**: `a212e88`

**发布日期**: 2026-04-20

### 实现功能

#### 1. 事件相机可视化系统
- 多格式数据支持：AEDAT4、RAW、H5/HDF5
- 事件流实时渲染（多种配色方案）
- 实时相机采集功能

#### 2. 数据处理管道
- 下采样与归一化
- 兴趣区域（ROI）裁剪
- 时间轴缩放与归一化
- 支持 640x480 和 1280x720 分辨率
- 针对不同数据集的裁剪处理（ini30、seet、RAW 1280x720）

#### 3. 神经网络推理
- EventMamba 模型集成
- WSL 端推理服务（linux_backend.py）
- Windows ↔ WSL ZMQ 通信
- 实时目标点预测

#### 4. 图形界面
- PyQt6 主窗口
- 文件选择与加载
- 参数配置（FPS、配色方案）
- ROI 窗口选择
- 预测结果圆形标记可视化

#### 5. 录制功能
- 支持 .raw 格式事件录制
- 开始/停止录制控制

---

## v2

**提交**: `ed03bc2`

**发布日期**: 2026-04-20

### 新增/优化功能

#### 1. RAW 离线回放速度修正
- 使用 `LiveReplayEventsIterator` 替代 `EventsIterator` 读取离线 RAW 文件
- 按事件时间戳进行实时回放，避免播放画面像开了倍速
- 实时相机保持原有实时读取逻辑

#### 2. 预测结果与图像帧时间同步
- 推理事件包新增 `timestamp` 字段（当前推理事件片段的结束时间戳）
- 新增 `cropped` 标记（标记模型输入是否来自裁剪区域）
- UI 端按时间戳缓存和匹配预测结果，预测点更准确

#### 3. 1280x720 分辨率下预测点绘制修正
- 预测点绘制按实际显示分辨率动态缩放
- 预测点与图像一起缩放，不会因窗口缩放产生坐标偏移
- 支持任意显示分辨率正确绘制

#### 4. 空事件与裁剪空结果保护
- 输入事件为空时直接跳过
- 裁剪后没有有效事件时直接跳过
- 不满足模型所需点数时不发送推理请求

#### 5. ZMQ 通信超时与异常处理
- ZMQ socket 在线程内部创建，避免跨线程使用风险
- 增加 `SNDTIMEO` 和 `RCVTIMEO` 超时设置
- 通信超时时向 UI 输出提示
- socket 异常后自动重建连接

#### 6. 模型加载失败保护
- 权重文件不存在时直接报错
- 权重加载失败时直接抛出异常
- 支持常见 checkpoint 包装格式（`state_dict`、`model_state_dict`、`model`）
- 自动选择 `cuda` 或 `cpu` 设备

#### 7. WSL 配置可维护性提升
- WSL 发行版名和 Linux Python 路径可通过环境变量覆盖
  - `EVENTMAMBA_WSL_DISTRO`
  - `EVENTMAMBA_LINUX_PYTHON`
- 停止后端时指定 WSL 发行版执行 `pkill`

#### 8. Qt 组件与函数命名规范化
- UI 组件命名统一（如 `start_btn` → `start_camera_button`）
- Python 函数命名规范化（如 `choose_file` → `select_input_file`）
- 数据处理函数重命名（如 `downSampling_cropping_and_normalization` → `downsample_crop_normalize_events`）

#### 9. UI 使用体验优化
- 停止相机或文件播放时，录制按钮恢复为"开始录制"
- 停止模型时清空预测缓存
- 日志输出限制最大行数
- 文件选择框增加默认目录
- ROI 窗口和按钮文案改为中文语义化文字

---

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Windows 端                              │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌──────────────┐   │
│  │  Qt UI      │    │ CameraThread │    │ NNWorker     │   │
│  │  widget.py  │◄───│ (事件采集)   │───►│ (数据预处理)  │   │
│  └─────────────┘    └─────────────┘    └──────┬───────┘   │
│                                                │            │
│                        ┌───────────────────────┘            │
│                        ▼                                    │
│               ┌─────────────────┐    ┌──────────────────┐  │
│               │  NetworkThread  │───►│   ZMQ Socket     │  │
│               │  (ZMQ Client)   │    │   (Req/Rep)     │  │
│               └─────────────────┘    └────────┬─────────┘  │
└──────────────────────────────────────────────┼─────────────┘
                                               │ TCP
┌──────────────────────────────────────────────┼─────────────┐
│                      WSL2 端                  │             │
├──────────────────────────────────────────────┼─────────────┤
│                        ┌─────────────────────┘             │
│                        ▼                                    │
│               ┌─────────────────┐    ┌──────────────────┐  │
│               │ linux_backend   │───►│ EventMamba       │  │
│               │ (ZMQ Server)   │◄───│ Predictor        │  │
│               └─────────────────┘    └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 贡献者

- GuoHR03

## 许可证

MIT License
