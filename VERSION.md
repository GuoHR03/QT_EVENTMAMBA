# 版本历史 (Version History)

本文档介绍每个版本实现的功能和变更。

---

## v1 (当前版本)

**提交**: `a212e88`

**发布日期**: 2026-04-20

### 实现功能

#### 1. 事件相机可视化系统
- 多格式数据支持：AEDAT4、RAW、H5/HDF5
- 事件流实时渲染（多种配色方案）
- 实时相机采集功能

#### 2. 数据处理管道
- 下采样与归一化
- 兴趣区域（ROI）裁剪
- 时间轴缩放与归一化
- 支持 640x480 和 1280x720 分辨率
- 针对不同数据集的裁剪处理（ini30、seet、RAW 1280x720）

#### 3. 神经网络推理
- EventMamba 模型集成
- WSL 端推理服务（linux_backend.py）
- Windows ↔ WSL ZMQ 通信
- 实时目标点预测

#### 4. 图形界面
- PyQt6 主窗口
- 文件选择与加载
- 参数配置（FPS、配色方案）
- ROI 窗口选择
- 预测结果圆形标记可视化

#### 5. 录制功能
- 支持 .raw 格式事件录制
- 开始/停止录制控制

---

## 历史版本

### v3
**提交**: `67c06b3`

**功能**: 重构相机和推理架构

- 统一文件输入频率控制
- H5 和 AEDAT4 模式现在使用 nn_interval_ms 而非 fps
- 神经网络推理与 UI 渲染分离

### v2
**提交**: `5a022e8`

**功能**: 优化代码结构

- 改进代码组织
- 初步支持神经网络推理

### Initial Commit
**提交**: `f7cee84`

**功能**: 初始版本

- Camera UI 代码
- Metavision SDK 集成
- 基础事件可视化

---

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Windows 端                              │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌──────────────┐   │
│  │  Qt UI      │    │ CameraThread │    │ NNWorker     │   │
│  │  widget.py  │◄───│ (事件采集)   │───►│ (数据预处理)  │   │
│  └─────────────┘    └─────────────┘    └──────┬───────┘   │
│                                                │            │
│                        ┌───────────────────────┘            │
│                        ▼                                    │
│               ┌─────────────────┐    ┌──────────────────┐  │
│               │  NetworkThread  │───►│   ZMQ Socket     │  │
│               │  (ZMQ Client)   │    │   (Req/Rep)     │  │
│               └─────────────────┘    └────────┬─────────┘  │
└──────────────────────────────────────────────┼─────────────┘
                                               │ TCP
┌──────────────────────────────────────────────┼─────────────┐
│                      WSL2 端                  │             │
├──────────────────────────────────────────────┼─────────────┤
│                        ┌─────────────────────┘             │
│                        ▼                                    │
│               ┌─────────────────┐    ┌──────────────────┐  │
│               │ linux_backend   │───►│ EventMamba       │  │
│               │ (ZMQ Server)    │◄───│ Predictor        │  │
│               └─────────────────┘    └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 数据处理流程

```
原始事件 (x, y, t, p)
        │
        ▼
┌───────────────────┐
│ 下采样 + 裁剪     │  ← 根据数据集类型选择处理函数
│ + 归一化          │
└───────────────────┘
        │
        ▼
归一化数据 (x_norm, y_norm, t_norm) ∈ [0, 1]
        │
        ├──► 神经网络推理 ──► 预测坐标 (x_pred, y_pred)
        │
        └──► UI 显示 ──► 圆形标记
```

## 贡献者

- GuoHR03

## 许可证

MIT License
